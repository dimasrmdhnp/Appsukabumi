import React from 'react';
import {
    Text,
    View,
    Platform,
    SafeAreaView,
    ScrollView,
    TouchableOpacity
} from 'react-native';

export default class KebijakanPrivasi extends React.Component {
    render() {
        return (
            <View>
            </View>
        )
    }
}